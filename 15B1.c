//Delete all duplicate elements from an array.
#include<stdio.h>
void main(){
	int n;
	printf("Enter the size of array:");
	scanf("%d",&n);
	int i,a[n];
	for(i=0;i<n;i++){
		printf("Enter the elements into array[i]:%d",i);
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++){
		if(a[i]==a[i+1]){
			printf()
		}
	}
}
